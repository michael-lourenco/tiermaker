'use client'

interface AdPlaceholderProps {
  className?: string
}

export function AdPlaceholder({ className }: AdPlaceholderProps) {
  // Retorna null para não mostrar nada quando não há publicidade
  // Isso mantém o layout limpo
  return null
}


